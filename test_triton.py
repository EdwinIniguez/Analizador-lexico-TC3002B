@mi_decorador
def prueba_triton(x, y) -> True:
    if x == 10:
        pass
    elif x != 20:
        continue
    else:
        break
    while x < 5:
        return False
    for i in [1, 2, 3]:
        a = 1 + 2 - 3 * 4 / 5 // 6 % 7 ** 8
        a += 1
        a -= 2
        a *= 3
        a /= 4
        b = (a <= 10) and (a >= 5) or not (a > 15) is True
        diccionario = {"llave": "valor_string"}
        objeto.metodo(x, y)
        c = ~a & b ` c ^ d << 1 >> 2
    return None
